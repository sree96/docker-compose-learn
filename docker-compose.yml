version: "3.1"

volumes: 
  postgresVolume:

services:
  db:
    image: postgres:latest
    volumes: 
      - postgresVolume:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: password
  hmaraserver:
    build: .
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/postgres
    volumes: 
      - .:/src/app/
    links: 
      - db
  hmaraserver2:
    build: .
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/postgres
    volumes: 
      - .:/src/app/
    links: 
      - db
  hmaraserver3:
    build: .
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/postgres
    volumes: 
      - .:/src/app/
    links: 
      - db
  mainserver:
    build: .
    ports:
      - 9000:9000
    volumes: 
      - .:/src/app/
    environment: 
      - servers=["http://compose_hmaraserver_1:8000", "http://compose_hmaraserver2_1:8000", "http://compose_hmaraserver3_1:8000"]
    command: ["nodemon", "/src/app/server.js"]
